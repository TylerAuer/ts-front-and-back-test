{
  "name": "ts-front-and-back-test",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start:tsc-watch": "tsc -w",
    "start:server-start-watch": "nodemon public/index.js",
    "start": "run-p start:*",
    "build:empty-public": "rm -rf public",
    "build:client": "cd app && npm run build",
    "build:move-client": "mkdir public && mv app/build public/client",
    "build:server": "tsc",
    "build": "run-s build:* --print-label",
    "postinstall": "cd app && npm install"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.17.1"
  },
  "devDependencies": {
    "@types/express": "^4.17.9",
    "@types/node": "^14.14.9",
    "npm-run-all": "^4.1.5",
    "typescript": "^4.1.2"
  }
}